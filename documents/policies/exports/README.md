# Policy exports for Plaid Production Access uploads

This directory contains uploadable copies of the four canonical compliance policies.

Source of truth lives in `documents/policies/*.md`. Do not edit files here directly — they are generated by `npm run export:policy-pdfs` (or `npx tsx scripts/export-policy-pdfs.ts`).

## What gets generated

For each of the four policies, the script emits:

- `*.pdf` — Letter-format PDF rendered via headless Chromium (uses the system Chromium installed at the path in `scripts/export-policy-pdfs.ts`; override with the `CHROMIUM_PATH` env var). This is the file to upload to Plaid.
- `*.html` — self-contained styled HTML with embedded CSS, kept alongside the PDF as a human-readable reference and as a fallback that can be opened in any browser and re-printed to PDF if needed.

## Files

| Policy | HTML | PDF | Source |
|---|---|---|---|
| Information Security Policy | `information-security-policy.html` | `information-security-policy.pdf` | `../information-security-policy.md` |
| Privacy Policy | `privacy-policy.html` | `privacy-policy.pdf` | `../privacy-policy.md` |
| Access Controls Policy | `access-controls-policy.html` | `access-controls-policy.pdf` | `../access-controls-policy.md` |
| Data Retention and Disposal Policy | `data-retention-policy.html` | `data-retention-policy.pdf` | `../data-retention-policy.md` |

## Plaid upload mapping

See `documents/plaid/security-questionnaire-answers.md` for the exact field-by-field mapping of these files into the Plaid Production Access form.
