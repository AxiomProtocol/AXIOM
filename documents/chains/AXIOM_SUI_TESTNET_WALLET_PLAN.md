# AXIOM SUI — TESTNET WALLET PLAN

**Document type:** Operations Plan  
**Phase:** Phase 5 — Testnet Claim Contract Prototype Design  
**Chain:** Sui (non-EVM, Move VM)  
**Date:** 2026-05-15  
**G04 Status:** ADDRESS RECORDED — awaiting private key in Replit Secrets  
**Classification:** Internal — operations record  

---

## 1. Overview

This document defines the testnet wallet requirements for Sui Phase 6
(testnet build and deployment). No wallet is provisioned in Phase 5.
No private keys appear in this document or any source file.

All wallets described here are testnet-only.
No mainnet Sui wallet will be activated without a separate Phase 7 authorization.

---

## 2. Wallet Requirements

### 2.1 Deployer / Admin Wallet (Testnet)

One wallet is required for Phase 6 testnet operations:

| Property | Requirement |
|---|---|
| Network | Sui Testnet (or Devnet) |
| Key type | ed25519 (recommended) or secp256k1 |
| Purpose | Package publication, AdminCap holder, campaign funding |
| Custody | Internal operator — NOT BitGo (BitGo does not support Sui) |
| Format | Single keypair generated by Sui CLI |
| Storage | Replit Secret (`SUI_TESTNET_ADMIN_PRIVATE_KEY`) — never committed to repo |

### 2.2 Test Claimant Wallets (Testnet)

For testing the claim flow, 2–3 additional testnet wallets are needed:

| Purpose | Count | Source |
|---|---|---|
| Valid claimant (in merkle tree) | 2 | Sui CLI or browser wallet |
| Invalid claimant (not in tree) | 1 | Sui CLI or browser wallet |

These do not require private key storage — they can be ephemeral test wallets
generated for each test run. Test claimant addresses must be recorded for
merkle tree construction.

---

## 3. Wallet Generation Method

### Recommended: Sui CLI

```bash
# Install Sui CLI (Rust-based, requires cargo)
cargo install --locked --git https://github.com/MystenLabs/sui.git \
  --branch testnet sui

# Generate a new keypair
sui keytool generate ed25519

# Output: address, publicKey, keyScheme
# Private key is stored in Sui CLI keystore (~/.sui/sui_config/sui.keystore)
```

**IMPORTANT:** Extract the private key from the keystore and store it
in Replit Secrets as `SUI_TESTNET_ADMIN_PRIVATE_KEY`. Never commit the
keystore file to the repository. Add `~/.sui/` to `.gitignore` if using
Sui CLI locally.

### Alternative: Sui Browser Wallet (Mysten Labs)

The official Sui Wallet browser extension can generate a keypair and
export the private key. Suitable for quick testnet testing.

---

## 4. Faucet Requirements

### Sui Testnet Faucet

Testnet SUI (not real value) is required for gas on all testnet transactions.

| Faucet | URL |
|---|---|
| Testnet faucet | https://faucet.testnet.sui.io/ |
| Devnet faucet | https://faucet.devnet.sui.io/ |

**Amount needed per operation (approximate):**
- Package publication: ~0.5–1 SUI (testnet)
- Campaign funding transaction: ~0.01 SUI (testnet)
- Individual claim transaction: ~0.001 SUI (testnet)

Faucet SUI has no monetary value. Request freely as needed for testing.

---

## 5. Environment Variables

The following env vars will be needed in Phase 6. They are defined here
for planning purposes. Do NOT set them until Phase 6 begins.

| Variable | Purpose | Required | Phase |
|---|---|---|---|
| `SUI_TESTNET_ADMIN_PRIVATE_KEY` | Admin wallet private key (testnet only) | Yes | Phase 6 |
| `SUI_RPC_URL` | Testnet RPC endpoint override (optional) | No | Phase 6 |

Testnet RPC endpoints:
- Testnet: `https://fullnode.testnet.sui.io`
- Devnet: `https://fullnode.devnet.sui.io`

Note: `SUI_RPC_URL` already exists as a defined variable in the Phase 4 scaffold
(`lib/chains/providers.ts`). At Phase 6, set it to the testnet endpoint.

**Do NOT set these variables until Phase 6 is authorized (G06 signed).**

---

## 6. Address Management

### Address Format
Sui addresses are 32-byte hex strings prefixed with `0x`:
```
0x<64 hex characters>
```
Example: `0x1a2b3c...` (64 hex chars)

### Address Recording
When the Phase 6 deployer wallet is provisioned:
1. Record the testnet address in this document (Section 7)
2. Store the private key in Replit Secrets as `SUI_TESTNET_ADMIN_PRIVATE_KEY`
3. Do NOT store the private key in this document or any code file
4. Fund the address via the testnet faucet
5. Confirm balance via Suiscan Testnet: https://testnet.suiscan.xyz/

---

## 7. Wallet Address Registry (Testnet)

Populated when wallet is provisioned in Phase 6.

| Role | Address | Funded | Date |
|---|---|---|---|
| Deployer / Admin | 0x4917ffea5289fba211976448c50103ba96a86e49a57e4dd1f22222c3b412e5ad | Pending faucet | 2026-05-15 |
| Test Claimant A | PENDING | No | — |
| Test Claimant B | PENDING | No | — |
| Test Invalid Claimant | PENDING | No | — |

---

## 8. Security Expectations (Testnet)

Testnet wallets hold no real value. However, the following practices
apply regardless:

- Private keys stored only in Replit Secrets — never in code, docs, or logs
- Testnet admin wallet private key not shared outside the operations team
- Wallet access revoked or rotated if any key exposure occurs
- The testnet wallet private key format must not appear in any `console.log`
  or server log output

---

## 9. Mainnet Wallet — Explicitly Deferred

No Sui mainnet wallet will be provisioned in Phase 5 or Phase 6.

Mainnet wallet activation requires:
- Phase 7 authorization signed by ops
- Separate custody design decision (not BitGo — Sui not supported)
- Legal review of mainnet distribution operations
- Separate mainnet env var set (`SUI_MAINNET_ADMIN_PRIVATE_KEY`)

**Do NOT use a testnet private key on Sui Mainnet.**

---

## 10. G04 Status

**G04: Testnet Wallet — PENDING**

No wallet provisioned as of Phase 5 design.
G04 is satisfied when:
- Deployer wallet address is recorded in Section 7 of this document
- Address is funded via testnet faucet
- Private key is confirmed stored in Replit Secrets (not verified here)
- Phase 6 authorization (G06) is signed

G04 update date: PENDING

### G04 Environment Check (2026-05-15)

| Check | Result |
|---|---|
| SUI_TESTNET_ADMIN_PRIVATE_KEY present in Replit Secrets | NOT SET |
| Sui CLI available in environment | NOT INSTALLED |
| Public address derivable | NOT POSSIBLE (no key, no CLI) |
| @mysten/sui SDK available for key derivation | NOT INSTALLED |

**Conclusion:** No wallet has been provisioned. No secret has been set.
No Sui CLI or SDK tooling is available to generate or verify a wallet.

**Required operator actions to satisfy G04:**
1. Install Sui CLI on your local machine (see Section 11 checklist).
2. Generate ed25519 keypair: `sui keytool generate ed25519`
3. Record public address in Section 7 of this document.
4. Store private key in Replit Secrets as `SUI_TESTNET_ADMIN_PRIVATE_KEY`.
5. Do NOT install Sui CLI in the Replit container — the CLI is not needed
   server-side. The secret alone is sufficient for Phase 6 TypeScript use.

G04b (faucet funding) cannot be attempted until G04 is satisfied.

---

## 11. Operator Execution Checklist — Phase 6 Wallet Provisioning

**Status: NOT_STARTED — do not execute until Phase 6 authorization (G06) is signed**

This checklist is for the operator who will provision the testnet wallet.
Complete every step in order. Do not skip or reorder steps.

---

### Step 1 — Install Sui CLI

The Sui CLI is required to generate an ed25519 keypair.

Option A — Install via Homebrew (macOS):
```
brew install sui
```

Option B — Install via Cargo (all platforms):
```
cargo install --locked --git https://github.com/MystenLabs/sui.git \
  --branch testnet sui
```

Option C — Download prebuilt binary from Sui GitHub releases:
```
https://github.com/MystenLabs/sui/releases
```

Verify installation:
```
sui --version
```

Expected output: `sui x.y.z-<commit>` (exact version varies)

---

### Step 2 — Generate ed25519 Testnet Wallet

Generate a new keypair. Do NOT reuse any existing wallet:
```
sui keytool generate ed25519
```

Expected output includes:
- address: 0x<64 hex chars>
- publicKey: <base64>
- keyScheme: ed25519
- peerId: <hex>

The private key is stored in the local Sui keystore file at:
  `~/.sui/sui_config/sui.keystore`

---

### Step 3 — Record Public Address Only

Record the public address (0x...) here in Section 7 of this document.
Do NOT record the private key, mnemonic, or keystore path in this document.

Update the Wallet Address Registry table in Section 7:
- Role: Deployer / Admin
- Address: <paste 0x address>
- Funded: No (will update after Step 5)
- Date: <today's date>

---

### Step 4 — Store Private Key in Replit Secrets

Extract the private key from the Sui keystore:
```
cat ~/.sui/sui_config/sui.keystore
```

The keystore is a JSON array of base64-encoded keys.
The key corresponding to the address generated in Step 2 must be
stored in Replit Secrets as `SUI_TESTNET_ADMIN_PRIVATE_KEY`.

**SECURITY RULES — no exceptions:**
- Do NOT paste the private key into any document, code file, or commit
- Do NOT log it to the console or server output
- Do NOT share it in chat, email, or messaging
- Do NOT commit `~/.sui/sui_config/sui.keystore` to the repository
  (add `~/.sui/` to `.gitignore` if running CLI locally)
- If the key is exposed accidentally, rotate it immediately:
  generate a new keypair and update the secret

Verification (without revealing the key):
- Confirm the secret name `SUI_TESTNET_ADMIN_PRIVATE_KEY` appears in
  the Replit Secrets panel
- Confirm the address in Section 7 matches the address that corresponds
  to the stored key (verify using `sui keytool show-address`)

---

### Step 5 — Fund with Testnet SUI Faucet

Request testnet SUI (not real value) via the faucet:

Primary faucet:   https://faucet.testnet.sui.io/
Alternate:        https://discord.com/channels/916379725201563759 (#testnet-faucet)

Request at least 1 SUI (testnet) to cover:
- Package publication: ~0.5–1 SUI
- Several test transactions: ~0.05 SUI total

---

### Step 6 — Verify Balance

Confirm balance is funded via Suiscan Testnet:
```
https://testnet.suiscan.xyz/account/<your-0x-address>
```

Or via Sui CLI:
```
sui client balance
```

Expected: balance > 0 testnet SUI

---

### Step 7 — Update G04 and G04b in Gate Tracker

Once Steps 1–6 are complete, update the Phase 6 gate tracker:
- G04: PENDING → CONFIRMED (wallet provisioned)
- G04b: PENDING → CONFIRMED (faucet funding confirmed)

File: `documents/chains/AXIOM_SUI_PHASE6_GATE_TRACKER.md`

---

### Security Reminders

- This wallet holds no real monetary value (testnet only)
- Never use this testnet private key on Sui Mainnet
- Never generate a Sui Mainnet wallet until Phase 7 is authorized
- If this checklist is followed by a contractor, the operator must
  verify the secret is stored correctly before proceeding to G06
