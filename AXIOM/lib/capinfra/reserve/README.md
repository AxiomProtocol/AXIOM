# Reserve (Phase 3)

Reserve service (canonical reserve snapshots, coverage ratio,
warning-level computation) is out of scope for Phase 1. The
`cap_reserve_snapshots` table exists; the operator asset-summary
endpoint returns nullable reserve fields in Phase 1.
