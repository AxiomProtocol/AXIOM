# Risk (Phase 3)

Risk service (Sentinel hooks, decision persistence) is out of scope for
Phase 1. The `cap_risk_decisions` and `cap_risk_policies` tables exist
so Phase 3 can hook in without a schema change.
