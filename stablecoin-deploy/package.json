{
  "name": "axusd-stablecoin-deploy",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network arbitrum",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network arbitrumSepolia",
    "verify": "hardhat run scripts/verify.ts --network arbitrum"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.19.0"
  }
}
